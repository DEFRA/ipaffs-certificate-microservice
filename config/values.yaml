service: certificate-service

container:
  replicas: 1

config:
  JDK_JAVA_OPTIONS: "-XX:MaxRAMPercentage=60.0"

resources:
  requests:
    cpu: 250m
    memory: 700Mi
  limits:
    cpu: 2
    memory: 700Mi

database:
  enabled: false
  migrations:
    enabled: false